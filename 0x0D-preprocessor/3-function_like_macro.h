#ifndef LIKEMACRO_H
#define LIKEMACRO_H
/**
 * File: 3-function_like_macro
 *
 * @x: input paramter value (int)
 *
 * Description: header file that computes the absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
